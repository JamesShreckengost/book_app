<!DOCTYPE html> 
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
        
  <title>Form</title>
</head>

  <body>
   <nav>Jim's Books
     <%= count %>
   </nav>

    <ul>
        <% bookArr.forEach(allBooks => { %>
          <li>
            <h2><%= allBooks.title %></h2>
            <p><%= allBooks.description %></p>
            <a href="/books/<%= allBooks.id %>"><button>View Details</button></a>
          </li>
       <% })   %>
    </ul>
  </body>

</html>